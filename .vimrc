set nocp

syntax enable
set background=dark
set t_Co=16
colorscheme solarized

set mouse=a
set incsearch
set hlsearch
set number

"Some people use two spaces as their tabs, some people are wrong.
set tabstop=4 "cosmetic
set shiftwidth=4 "use > and < keys to indent regions
set autoindent
set expandtab
set softtabstop=4

